{
  "name": "restaurant-chatbot",
  "version": "1.0.0",
  "scripts": {
    "start": "tsx backend/index.ts",
    "postinstall": "cd backend && npm install && npx prisma generate && npx prisma migrate deploy && cd ../frontend && npm install && npm run build",
    "seed": "tsx backend/prisma/seed.ts"
  },
  "engines": {
    "node": "20.x"
  }
}